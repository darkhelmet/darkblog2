defaults: &defaults
  allow_dynamic_fields: false
  # slaves:
  #   - host: slave1.local
  #     port: 27018
  #   - host: slave2.local
  #     port: 27019

development:
  host: localhost
  <<: *defaults
  database: darkblog2_development

test:
  host: localhost
  <<: *defaults
  database: darkblog2_test

# set these environment variables on your prod server
production:
  <<: *defaults
  uri: <%= ENV['MONGOHQ_URL'] %>